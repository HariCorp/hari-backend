export enum OrderStatus {
  CREATED = 'created',
  CONFIRMED = 'confirmed',
  SHIPPING = 'shipping',
  SHIPPED = 'shipped',
  CANCELED = 'canceled',
  RETURNED = 'returned',
  REFUNDED = 'refunded',
  FAILED = 'failed',
  COMPLETED = 'completed',
}
